{
  "name": "root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/*/examples/*"
  ],
  "scripts": {
    "prepare": "npm run prepare --workspace=packages",
    "test": "npm test --workspace=packages"
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}

{
  "name": "root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "packages/*/examples/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages",
    "test": "npm test --workspace=packages"
  },
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}
